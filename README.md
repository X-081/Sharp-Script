# Sharp Script

Make sure you have at least version 10 of .Net installed first.

This program can help you create a right-click menu that allows you to run csharp code as script.

It's equivalent to this
```bash
dotnet run --file your_file_path
```